# Dependencies
node_modules/

# Build output
dist/
out/
electron/dist/
mobile/worklet/dist/entry.flat.cjs

# Pear local staging cache (mirrors what's already published at the `upgrade` link)
linda-pear-1.0.0/

# Local dev identities / test corestore data — never share private key material
.dev-storage/

# Native build artifacts (regenerated by `expo run:android` / `expo run:ios`)
mobile/android/app/build/
mobile/android/.gradle/
mobile/android/local.properties
mobile/android/app/.cxx/
mobile/ios/build/
mobile/ios/Pods/
mobile/.expo/

# Tool-local caches, not project source
.claude/
.headroom/
.tokensave/
.serena/

# OS / editor cruft
.DS_Store
Thumbs.db
